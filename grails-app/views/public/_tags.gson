import cava.tags.Tag

model {
    Integer max
    Integer offset
    Integer resultCount
    List<Tag> tagList = []
}

json {
    max max
    offset offset
    resultCount resultCount
    results g.render(tagList, [deep:true]) {}
}